<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">\
    
    <session-config>
        <session-timeout>30</session-timeout>
    </session-config>  
    
    <listener>
        <listener-class>hibernate.WebappListener</listener-class> <!--Registers Listner with TomCat-->
    </listener>
    
    <servlet>
        <servlet-name>AccountController</servlet-name>
        <servlet-class>Controllers.FrontAccountController</servlet-class>
        <init-param>
            <param-name>createTables</param-name>
            <param-value>true</param-value> <!-- true = doesn't save data if server is restarted = create tables... use this only once when creating tables. -->
        </init-param>                        <!-- fales = saves data to DB even if server is restarted = DONT create tables -->
    </servlet>
    <servlet-mapping>
        <servlet-name>AccountController</servlet-name>
        <url-pattern>/Controllers/FrontAccountController</url-pattern>
    </servlet-mapping>
    
</web-app>

